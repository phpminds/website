{% extends "logged-base.twig" %}

{% block title %}Event Details{% endblock %}

{% block content %}

    <h3>Event Details</h3>

    <div class="section-container tabs" data-section>
        <section class="section">
            {% if errors|length %}
                <div data-alert class="alert-box alert round">
                    <ul>
                        {% for error in errors %}
                            <li>{{ error }}</li>
                        {% endfor %}
                    </ul>

                    <a href="#" class="close">&times;</a>
                </div>
            {% endif %}

            {% if meetup_event %}
            <h5 class="title"><a href="#panel1">{{ meetup_event.subject }}</a></h5>
            <div class="content" data-slug="panel1">

                <p>This event has been scheduled for {{ meetup_event.date_time }}</p>

                <p>The event on meetup can be found at: <a href="{{ meetup_event.event_url }}" target="_blank">{{ meetup_event.event_url }}</a></p>

                <p>A talk has also been created on Joind.in and can be found at: <a href="{{ event_info.joindin_url }}" target="_blank">{{ event_info.joindin_url  }}</a></p>

                <p>It will be held at: <a href="https://maps.google.com/?q={{ meetup_event.location }}" target="_blank">{{ meetup_event.location }}</a></p>
            </div>
            {% endif %}

        </section>
    </div>

{% endblock %}

{% block right_column %}

{% endblock %}

{% block scripts %}
    <script>
        $( document ).ready(function() {

        });
    </script>
{% endblock %}