{% extends "base.twig" %}
{% block content %}

		<div class="large-12 columns banner">
			<img class="phpminds-logo" src="imgs/phpminds-banner.svg" alt="phpminds - php usergroup" />
			<p> [ a witty strapline goes here ]</p>
		</div>
        
        <div class="row">
<div class="large-8 columns event">
<h4> <i class="fa fa-calendar"></i> Next Event </h4>
<p>{{ event.subject }} on {{ event.date_time }}</p>
        <div class="event">
            <p>{{ event.subject }}</p>
            <p>{{ event.description | raw }}</p>
            <p><a class="" href="{{ event.event_url }}">RSVP</a></p>
        </div>
</div>
<div class="large-4 columns">

</div>
</div>
<div class="large-12 columns sub-banner">
			<div class="row">
				<div class="large-6 columns">

				</div>
			</div>
</div>
	
        
        
{% endblock %}
    
        
