paths:
    migrations: %%PHINX_CONFIG_DIR%%/migrations

environments:
    default_migration_table: phinxlog
    default_database: phpminds

    development:
        adapter: mysql
        host: localhost
        name: phpminds
        user: root
        pass: Admin123
        port: 3306
        charset: utf8

